// const FadeProperties = new Map()

// FadeProperties.set(true, { opacity: 1, visibility: 'visible' })
// FadeProperties.set(false, { opacity: 0, visibility: 'hidden' })

// export type FadeInOutTransitionOptions = {
//     duration: number
// }

// export function buildFadeInOutAnimation({ duration }: FadeInOutTransitionOptions) {
//     const options = {
//         transition: `opacity ${duration}ms`
//     }
//     return (isVisible: boolean) => ({
//         ...options,
//         ...FadeProperties.get(isVisible)
//     })
// }
